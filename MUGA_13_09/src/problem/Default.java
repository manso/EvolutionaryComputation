/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package problem;

import problem.realCoded.CEC2008.F6_Shifted_Schaffer;

/**
 *
 * @author arm
 */
public class Default extends F6_Shifted_Schaffer {

    @Override
    public String toString() {
        return "(" + getClass().getSuperclass().getSimpleName() + ")" + super.toString();
    }

    @Override
    public String getName() {
        return "(" + getClass().getSuperclass().getSimpleName() + ")" + super.getName();
    }
}
